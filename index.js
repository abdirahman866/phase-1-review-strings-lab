// Define the currentUser variable with any name you like
const currentUser = 'Grace Hopper';

// Create the welcomeMessage using template literals
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// Make an excited version by making welcomeMessage uppercase
const excitedWelcomeMessage = welcomeMessage.toUpperCase();

// Create a short greeting that only uses the first letter of currentUser
const shortGreeting = `Welcome, ${currentUser.slice(0, 1)}!`;

